export type Skill = {
  id: number;
  name: string;
  category: string;
  icon_name: string;
  color: string;
};

export type SkillCategory = {
  id: number;
  slug: string;
  name: string;
  icon_name: string;
};
